// Assessment 1/Practica 1
//1. Desarrollar un algoritmo en scala que calcule el radio de un circulo (formula r=c/2pi)
val cir=10
val pi=3.1416
val r=cir/pi*2
println(r)

//2. Desarrollar un algoritmo en scala que me diga si un numero es primo

val numero = 4
var cont = 0
for(i <- Range(1, numero)) 
{
 if( numero % i == 0) 
 {
   cont = cont+1
 }
 }
if(cont != 2) 
{
 println("El numero no es primo")
} 
else 
{
 println("El numero es primo ")
}

//3. Dada la variable bird = "tweet", utiliza interpolacion de string para
//   imprimir "Estoy ecribiendo un tweet"
val bird = "tweet"
val interpolar = "Estoy escribiendo un "+ bird
